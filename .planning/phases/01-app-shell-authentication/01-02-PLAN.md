---
phase: 01-app-shell-authentication
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - frontend/src/store/authApi.ts
  - frontend/src/lib/api/validation-errors.ts
  - frontend/src/components/auth/GuestAuthGuard.tsx
  - frontend/src/app/(auth)/layout.tsx
  - frontend/src/app/(auth)/register/page.tsx
  - frontend/src/app/(auth)/login/page.tsx
  - frontend/src/components/providers/StoreProvider.tsx
autonomous: true
requirements:
  - AUTH-FE-01
  - AUTH-FE-03
  - UX-02
must_haves:
  truths:
    - "User can register with email, password, displayName, tag and lands on /deeds logged in"
    - "User stays logged in after browser refresh until logout or token expiry"
    - "409 on register shows inline errors under email or tag field"
    - "400 validation maps to inline field errors on auth forms"
    - "Authenticated user visiting /login or /register is redirected to /deeds"
  artifacts:
    - path: frontend/src/app/(auth)/register/page.tsx
      provides: Registration form with tag helper and @ prefix UI
      contains: Create your Djosu account
    - path: frontend/src/components/auth/GuestAuthGuard.tsx
      provides: Inverse guard for auth routes
    - path: frontend/src/store/authApi.ts
      provides: register mutation
      contains: auth/register
  key_links:
    - from: frontend/src/app/(auth)/register/page.tsx
      to: POST /api/auth/register
      via: useRegisterMutation
      pattern: register
    - from: frontend/src/components/providers/StoreProvider.tsx
      to: GET /api/users/me
      via: getMe on bootstrap
      pattern: getMe
---

## Phase Goal

**As a** new user, **I want to** create an account and stay signed in after refresh, **so that** I do not re-enter credentials on every visit.

<objective>
Complete registration flow and session persistence: register mutation, register page per UI-SPEC, field-level 400/409 mapping, and guest redirect away from auth pages when already logged in.

Purpose: Satisfy AUTH-FE-01, AUTH-FE-03, and partial UX-02 before global ErrorBanner in Plan 03.
Output: Working /register, refresh hydration, inverse auth guard.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-app-shell-authentication/01-CONTEXT.md
@.planning/phases/01-app-shell-authentication/01-UI-SPEC.md
@.planning/phases/01-app-shell-authentication/01-PATTERNS.md
@backend/src/auth/dto/register.dto.ts
@backend/src/common/validators/tag.constants.ts
@backend/test/auth.e2e-spec.ts

<interfaces>
Register body: { email, password, displayName, tag } — tag normalized lowercase, pattern ^[a-z0-9_]{3,32}$
POST /api/auth/register → 201 AuthResponse; 409 message "Email or tag already exists"
Login cross-links and register copy per UI-SPEC copywriting contract.
D-11: valid token on /login or /register → redirect /deeds
D-14: 409 → inline under email or tag (detect via message substring)
D-15: 400 → inline field errors; banner only for non-field errors on auth pages
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register API endpoint and registration page</name>
  <files>frontend/src/store/authApi.ts, frontend/src/lib/api/validation-errors.ts, frontend/src/app/(auth)/register/page.tsx, frontend/src/app/(auth)/login/page.tsx</files>
  <read_first>backend/src/auth/dto/register.dto.ts, backend/src/common/validators/tag.constants.ts, backend/test/auth.e2e-spec.ts, .planning/phases/01-app-shell-authentication/01-UI-SPEC.md (register screen), frontend/src/app/(auth)/login/page.tsx</read_first>
  <acceptance_criteria>
    - authApi register mutation: POST auth/register, invalidates or sets credentials on success
    - Register page fields: email, password, displayName, tag with label "Username (tag)"
    - Tag helper muted text: "3–32 characters: lowercase letters, numbers, underscore"
    - Decorative @ prefix before tag input (not submitted); submit tag without @
    - Primary CTA "Create account"; loading "Creating account…"
    - Success: setCredentials + router.push /deeds (D-09)
    - 409: mapValidationErrors or dedicated mapper puts message under email if contains "email" (case-insensitive) else under tag
    - 400: mapValidationErrors assigns each ValidationPipe message to matching field key
    - Login page cross-link: "Don't have an account? Create account" → /register
    - Register cross-link: "Already have an account? Sign in" → /login
    - Forbidden button labels: Submit, OK, Save, Cancel per UI-SPEC
  </acceptance_criteria>
  <action>
    Extend validation-errors.ts with mapConflictError(message: string) for 409 per D-14. Reuse AuthCard, TextField, PrimaryButton from Plan 01. Client component page. Do not add AppNav. On non-field API errors for register/login, use local page-level error state string above card (ErrorBanner global wiring is Plan 03).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>grep -q "auth/register" frontend/src/store/authApi.ts && grep -q "Create your Djosu account" frontend/src/app/\(auth\)/register/page.tsx</automated>
  </verify>
  <done>Register compiles; duplicate email or tag shows inline field error not only console</done>
</task>

<task type="auto">
  <name>Task 2: Session hydration on refresh and guest auth guard</name>
  <files>frontend/src/components/providers/StoreProvider.tsx, frontend/src/components/auth/GuestAuthGuard.tsx, frontend/src/app/(auth)/layout.tsx</files>
  <read_first>.planning/phases/01-app-shell-authentication/01-CONTEXT.md (D-02, D-03, D-11), frontend/src/components/auth/AuthGuard.tsx, frontend/src/store/authSlice.ts</read_first>
  <acceptance_criteria>
    - On app load with valid token: getMe succeeds → user populated in auth slice; isHydrating false
    - On getMe 401 after stored token: clearSession; user lands on login when visiting protected routes
    - Refresh on /deeds: user remains authenticated when token valid (AUTH-FE-03)
    - GuestAuthGuard: if accessToken and user present, redirect to /deeds; if token without user wait for hydration
    - (auth)/layout wraps children with GuestAuthGuard
    - No duplicate getMe loops on every render — single bootstrap effect in StoreProvider
  </acceptance_criteria>
  <action>
    Implement per D-02, D-11. GuestAuthGuard is "use client" mirror of backend @Public() routes inverse. Harden StoreProvider: handle getMe error by clearSession. Do not implement logout button yet (Plan 03). Ensure AuthGuard and GuestAuthGuard both respect isHydrating to avoid redirect flash.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>grep -q "GuestAuthGuard" frontend/src/components/auth/GuestAuthGuard.tsx && grep -q "getMe" frontend/src/components/providers/StoreProvider.tsx</automated>
  </verify>
  <done>Refresh keeps session; logged-in user cannot open /login or /register (redirects to /deeds)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Register form → API | User-supplied tag/email; server validates with IsTag/IsEmail |
| Conflict responses | 409 reveals email/tag taken — acceptable for registration UX per D-14 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Information Disclosure | 409 inline errors | accept | Intentional per D-14; only on register form not public enumeration endpoint |
| T-01-08 | Tampering | Tag input | mitigate | Submit normalized tag; mirror TAG_PATTERN client-side for immediate feedback optional but server is authoritative |
| T-01-09 | Spoofing | getMe bootstrap | mitigate | Invalid/expired token cleared via 401 handler (D-03) |
| T-01-SC | Tampering | npm installs | mitigate | No new packages this plan |
</threat_model>

<verification>
- Register new user → /deeds with token in localStorage
- Browser refresh on /deeds → still authenticated
- Visit /login while logged in → redirect /deeds
- Duplicate register → inline field error
</verification>

<success_criteria>
AUTH-FE-01 and AUTH-FE-03 satisfied; auth forms show field-level 400/409 errors per D-14/D-15.
</success_criteria>

<output>
Create `.planning/phases/01-app-shell-authentication/01-02-SUMMARY.md` when done
</output>

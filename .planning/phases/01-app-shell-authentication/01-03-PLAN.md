---
phase: 01-app-shell-authentication
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - frontend/src/store/authSlice.ts
  - frontend/src/store/baseApi.ts
  - frontend/src/store/uiSlice.ts
  - frontend/src/store/index.ts
  - frontend/src/components/layout/AppNav.tsx
  - frontend/src/components/feedback/ErrorBanner.tsx
  - frontend/src/components/ui/TextButton.tsx
  - frontend/src/app/(app)/layout.tsx
  - frontend/src/app/(app)/friends/page.tsx
  - frontend/src/app/(app)/settings/page.tsx
  - frontend/src/app/(auth)/login/page.tsx
autonomous: true
requirements:
  - AUTH-FE-04
  - AUTH-FE-05
  - UX-02
  - UX-03
must_haves:
  truths:
    - "D-04: Top nav — Djosu left, section links center, @tag + Logout right"
    - "D-05: Nav links Deeds, Friends, Settings active (stub pages OK)"
    - "D-06: Mobile hamburger menu below md breakpoint"
    - "D-07: Header shows @tag (mono) and Log out TextButton"
    - "D-12: Global ErrorBanner for general API failures in app shell"
    - "D-13: 401 redirects to /login?reason=session_expired with session expired banner text"
    - "User can log out and loses access to protected routes"
    - "Responsive nav shows Deeds, Friends, Settings with active route styling"
    - "Stub pages /friends and /settings render UI-SPEC copy"
  artifacts:
    - path: frontend/src/components/layout/AppNav.tsx
      provides: Top nav 56px, links, hamburger, tag, logout
      min_lines: 40
    - path: frontend/src/components/feedback/ErrorBanner.tsx
      provides: role=alert dismissible banner
      contains: role="alert"
    - path: frontend/src/app/(app)/friends/page.tsx
      provides: Friends stub
    - path: frontend/src/app/(app)/settings/page.tsx
      provides: Settings stub
  key_links:
    - from: frontend/src/components/layout/AppNav.tsx
      to: authSlice clearSession
      via: Log out button
      pattern: clearSession
    - from: frontend/src/store/baseApi.ts
      to: uiSlice or authSlice banner message
      via: baseQuery error side effect
      pattern: setBannerMessage|setApiError
    - from: frontend/src/app/(auth)/login/page.tsx
      to: searchParams reason=session_expired
      via: ErrorBanner above card
      pattern: session_expired
---

## Phase Goal

**As a** signed-in user, **I want to** navigate the app shell, see API errors clearly, and log out safely, **so that** Phase 2–4 features plug into a complete layout.

<objective>
Ship full app shell: AppNav with responsive hamburger, ErrorBanner wired to RTK Query errors, logout, friends/settings stubs, session-expired login state, and polish login/register error banner behavior.

Purpose: Complete AUTH-FE-04/05, UX-02/03 and all nav decisions D-04–D-07, D-12–D-13.
Output: Production-ready Phase 1 shell matching UI-SPEC.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-app-shell-authentication/01-UI-SPEC.md
@.planning/phases/01-app-shell-authentication/01-CONTEXT.md
@.planning/phases/01-app-shell-authentication/01-PATTERNS.md

<interfaces>
App shell layout structure from UI-SPEC:
- AppNav h-14, bg-white dark:bg-zinc-900, border-b
- ErrorBanner full width below nav, role="alert", dismiss ×
- main max-w-3xl mx-auto p-6 lg:p-8

Copy (exact): Log out, Session expired — please log in again., nav labels Deeds/Friends/Settings, stub headings/bodies for friends and settings tables in UI-SPEC.

D-04 top nav layout; D-06 hamburger md breakpoint; D-07 @tag mono + Log out TextButton; D-12 global banner; D-13 session_expired query param.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App shell layout, navigation, and stub pages</name>
  <files>frontend/src/components/layout/AppNav.tsx, frontend/src/components/ui/TextButton.tsx, frontend/src/app/(app)/layout.tsx, frontend/src/app/(app)/friends/page.tsx, frontend/src/app/(app)/settings/page.tsx</files>
  <read_first>.planning/phases/01-app-shell-authentication/01-UI-SPEC.md (component inventory, app shell layout, stub tables, interaction states), frontend/src/app/(app)/deeds/page.tsx, frontend/src/app/(app)/layout.tsx</read_first>
  <acceptance_criteria>
    - (app)/layout renders AppNav + main children area per UI-SPEC shell diagram
    - AppNav left: "Djosu"; center: Link to /deeds, /friends, /settings with labels Deeds, Friends, Settings
    - Active route: text-base font-semibold + border-b-2 border-zinc-900 dark:border-zinc-50
    - Right: @tag in font-mono text-sm text-zinc-600 dark:text-zinc-400; Log out TextButton (D-07)
    - Mobile: hamburger below md; aria-label Open menu/Close menu; stacked links in panel; link click closes menu
    - nav element aria-label="Main"
    - friends stub: heading Friends + body copy from UI-SPEC
    - settings stub: heading Settings + body copy from UI-SPEC
    - deeds stub unchanged copy; all stubs use gap-8 heading/body layout
  </acceptance_criteria>
  <action>
    Implement per D-04, D-05, D-06. AppNav and layout client components where hooks needed. Use next/link and usePathname for active state. Inline SVG icons 20px for hamburger — no icon npm package. Do not wire ErrorBanner in this task (Task 2). Prefer @tag in header per D-07.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q "AppNav" frontend/src/components/layout/AppNav.tsx && rg -q "Friends" "frontend/src/app/(app)/friends/page.tsx" && rg -q "Settings" "frontend/src/app/(app)/settings/page.tsx"</automated>
  </verify>
  <done>Authenticated user sees full nav and can navigate between three stub sections; mobile menu toggles</done>
</task>

<task type="auto">
  <name>Task 2: Logout, global ErrorBanner, and session-expired UX</name>
  <files>frontend/src/store/uiSlice.ts, frontend/src/store/index.ts, frontend/src/store/baseApi.ts, frontend/src/store/authSlice.ts, frontend/src/components/feedback/ErrorBanner.tsx, frontend/src/app/(app)/layout.tsx, frontend/src/app/(auth)/login/page.tsx</files>
  <read_first>.planning/phases/01-app-shell-authentication/01-CONTEXT.md (D-03, D-12, D-13), .planning/phases/01-app-shell-authentication/01-UI-SPEC.md (ErrorBanner colors, session expired)</read_first>
  <acceptance_criteria>
    - Log out: dispatch clearSession, router.push /login; no confirmation modal (AUTH-FE-04)
    - After logout, visiting /deeds redirects to /login (AUTH-FE-05)
    - uiSlice (or authSlice field) holds bannerMessage: string | null; dismiss clears it
    - baseQuery wrapper on non-401 API errors (409 on non-register routes later, 5xx, network): dispatch banner with API message or fallback "Something went wrong. Try again or sign in again." (UX-02)
    - 401 on authenticated routes: clearSession + redirect /login?reason=session_expired with banner text "Session expired — please log in again." (D-13)
    - ErrorBanner: role="alert", red-50 / red-950/40 bg, dismiss ×, renders in (app)/layout below AppNav
    - Login page reads reason=session_expired from searchParams; shows same session expired message above card (D-13)
    - Login/register field errors remain inline; banner for non-field errors only on auth pages (D-15)
  </acceptance_criteria>
  <action>
    Wire AppNav logout per D-04. Extend baseApi reauth wrapper from Plan 01 — do not duplicate 401 logic. Register 409 should remain inline (do not global-banner 409 on register). Connect ErrorBanner to Redux selector. Session expired banner on login persists until dismiss or successful login per UI-SPEC interaction states.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q 'role="alert"' frontend/src/components/feedback/ErrorBanner.tsx && rg -q "session_expired" "frontend/src/app/(auth)/login/page.tsx" && rg -q "Log out" frontend/src/components/layout/AppNav.tsx</automated>
  </verify>
  <done>Logout works; forced 401 shows session expired on login; simulated 5xx shows global banner in app shell</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ErrorBanner message | May display API error strings — avoid rendering raw HTML; text content only |
| Logout | Client-side only — must clear localStorage to prevent token reuse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Information Disclosure | ErrorBanner API messages | mitigate | Render as text nodes only; strip/limit length if needed |
| T-01-11 | Spoofing | Logout | mitigate | clearSession removes djosu_access_token and Redux state |
| T-01-12 | Tampering | XSS via banner | mitigate | No dangerouslySetInnerHTML; React escapes by default |
| T-01-SC | Tampering | npm installs | mitigate | No new packages this plan |
</threat_model>

<verification>
- Full Phase 1 UAT: register, login, refresh, logout, protected redirect, nav between stubs, session expired flow
- `cd frontend && npm run build && npm run lint`
- Resize viewport &lt; md: hamburger works
</verification>

<success_criteria>
All Phase 1 ROADMAP success criteria and requirements AUTH-FE-01–05, UX-01–03 met; D-01–D-15 implemented.
</success_criteria>

<output>
Create `.planning/phases/01-app-shell-authentication/01-03-SUMMARY.md` when done
</output>

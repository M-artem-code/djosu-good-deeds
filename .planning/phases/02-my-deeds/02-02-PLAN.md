---
phase: 02-my-deeds
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - frontend/src/lib/api/validation-errors.ts
  - frontend/src/store/deedsApi.ts
  - frontend/src/components/ui/TextAreaField.tsx
  - frontend/src/components/deeds/AddDeedForm.tsx
  - frontend/src/app/(app)/deeds/page.tsx
autonomous: true
requirements:
  - DEED-02
  - DEED-05
must_haves:
  truths:
    - "D-06: Inline add form at top of /deeds"
    - "D-08: Add form collapsed by default; auto-expanded when list empty"
    - "D-10: Form fields title + optional description only"
    - "D-11: Add deed / Adding… button copy"
    - "User can create a deed with title and optional description"
    - "Whitespace-only title shows inline validation error without silent failure"
  artifacts:
    - path: frontend/src/components/deeds/AddDeedForm.tsx
      provides: Collapsible create form
      contains: Add deed
    - path: frontend/src/store/deedsApi.ts
      provides: createDeed mutation
      contains: createDeed
    - path: frontend/src/lib/api/validation-errors.ts
      provides: mapDeedValidationErrors
      contains: mapDeedValidationErrors
  key_links:
    - from: frontend/src/components/deeds/AddDeedForm.tsx
      to: POST /api/deeds
      via: useCreateDeedMutation
      pattern: createDeed
---

## Phase Goal

**As a** user with an empty or growing list, **I want to** add deeds inline with clear validation, **so that** I can record good deeds without leaving the page.

<objective>
Add collapsible AddDeedForm, createDeed mutation, TextAreaField, and deed-specific 400 mapping.

Purpose: Satisfy DEED-02 and DEED-05.
Output: Working create flow; list refreshes via cache invalidation after 201.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-my-deeds/02-CONTEXT.md
@.planning/phases/02-my-deeds/02-UI-SPEC.md
@.planning/phases/02-my-deeds/02-PATTERNS.md
@backend/src/deeds/dto/create-deed.dto.ts
@backend/test/deeds.e2e-spec.ts
@frontend/src/components/ui/TextField.tsx
@frontend/src/components/ui/PrimaryButton.tsx
@frontend/src/components/ui/TextButton.tsx

<interfaces>
POST /api/deeds body: { title: string, description?: string }
→ 201 DeedPublic; 400 on whitespace title or unknown fields (e.g. status)
Do NOT send status on create.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TextAreaField, mapDeedValidationErrors, createDeed mutation</name>
  <files>frontend/src/components/ui/TextAreaField.tsx, frontend/src/lib/api/validation-errors.ts, frontend/src/store/deedsApi.ts</files>
  <read_first>frontend/src/components/ui/TextField.tsx, frontend/src/lib/api/validation-errors.ts, .planning/phases/02-my-deeds/02-PATTERNS.md</read_first>
  <acceptance_criteria>
    - TextAreaField mirrors TextField: label, error, h-auto min-h, rows prop, same border/focus classes
    - mapDeedValidationErrors maps message to title/description keys
    - createDeed mutation POST deeds, invalidatesTags LIST
    - export useCreateDeedMutation
  </acceptance_criteria>
  <action>
    Add TextAreaField for optional description (max 500). Extend validation-errors without breaking auth mappers. Add createDeed to deedsApi.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q "createDeed" frontend/src/store/deedsApi.ts && rg -q "mapDeedValidationErrors" frontend/src/lib/api/validation-errors.ts</automated>
  </verify>
  <done>createDeed mutation and deed validation mapper ready</done>
</task>

<task type="auto">
  <name>Task 2: AddDeedForm wired on deeds page</name>
  <files>frontend/src/components/deeds/AddDeedForm.tsx, frontend/src/app/(app)/deeds/page.tsx</files>
  <read_first>.planning/phases/02-my-deeds/02-UI-SPEC.md, frontend/src/app/(auth)/register/page.tsx</read_first>
  <acceptance_criteria>
    - Collapsed: TextButton "Add deed" reveals form
    - When deeds.length === 0: form expanded by default (D-08 + D-21)
    - Fields: Title (TextField), Description (TextAreaField, optional helper text per UI-SPEC)
    - PrimaryButton "Add deed" / "Adding…"; TextButton "Cancel" collapses and clears fields
    - On 400: inline title error via mapDeedValidationErrors; fallback "Title is required"
    - On success: collapse form, clear fields, list shows new card (invalidates LIST)
    - No success toast
    - Do not send status in POST body
  </acceptance_criteria>
  <action>
    Place AddDeedForm below page title, above DeedList. Handle RTK error shape { data: { message } }. Client-side trim title before submit optional but server is source of truth for 400.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
    <automated>rg -q "Adding" frontend/src/components/deeds/AddDeedForm.tsx</automated>
  </verify>
  <done>User can add deed; whitespace title shows inline error</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-03 | Tampering | POST body extra fields | mitigate | Only title/description sent; API forbidNonWhitelisted |
</threat_model>

<verification>
- Manual: create deed with title only; create with description; submit whitespace title → inline error
</verification>

<success_criteria>
DEED-02 and DEED-05 satisfied.
</success_criteria>

<output>
Create `.planning/phases/02-my-deeds/02-02-SUMMARY.md` when done
</output>

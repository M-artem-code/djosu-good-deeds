---
phase: 02-my-deeds
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - frontend/src/store/deedsApi.ts
  - frontend/src/components/deeds/DeedCard.tsx
  - frontend/src/components/deeds/DeedList.tsx
  - frontend/src/components/deeds/DeleteDeedModal.tsx
  - frontend/src/app/(app)/deeds/page.tsx
autonomous: true
requirements:
  - DEED-03
  - DEED-04
must_haves:
  truths:
    - "D-07: Edit expands card inline with title + description"
    - "D-09: Cancel collapses edit without save"
    - "D-11: Save changes / Saving… copy on edit"
    - "D-12: Mark done on planned cards"
    - "D-13: Mark planned on done cards"
    - "D-14: No confirmation before status change"
    - "D-15: Status button disabled with Marking done… / Marking planned…"
    - "D-16: Delete requires modal confirmation"
    - "D-17: Delete control in card action row with destructive TextButton style"
    - "D-18: Modal copy Delete deed? / can't be undone / Delete deed / Keep deed"
    - "D-19: On delete 204 card removed via cache invalidation; no toast"
    - "User can edit deed fields and change status"
    - "User can delete a deed from the list"
  artifacts:
    - path: frontend/src/components/deeds/DeedCard.tsx
      provides: View/edit modes and action row
      contains: Mark done
    - path: frontend/src/components/deeds/DeleteDeedModal.tsx
      provides: Delete deed? confirmation
      contains: Keep deed
    - path: frontend/src/store/deedsApi.ts
      provides: updateDeed and deleteDeed mutations
      contains: updateDeed
  key_links:
    - from: frontend/src/components/deeds/DeedCard.tsx
      to: PATCH /api/deeds/:id
      via: useUpdateDeedMutation
      pattern: updateDeed
    - from: frontend/src/components/deeds/DeleteDeedModal.tsx
      to: DELETE /api/deeds/:id
      via: useDeleteDeedMutation
      pattern: deleteDeed
---

## Phase Goal

**As a** user managing deeds, **I want to** edit, change status, and delete with clear confirmations, **so that** my list stays accurate without accidental data loss.

<objective>
Complete CRUD: inline edit, status PATCH buttons, delete modal, updateDeed/deleteDeed mutations.

Purpose: Satisfy DEED-03 and DEED-04.
Output: Full Phase 2 deeds management on `/deeds`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-my-deeds/02-CONTEXT.md
@.planning/phases/02-my-deeds/02-UI-SPEC.md
@.planning/phases/02-my-deeds/02-PATTERNS.md
@backend/src/deeds/dto/update-deed.dto.ts
@backend/test/deeds.e2e-spec.ts

<interfaces>
PATCH /api/deeds/:id — { title?, description?, status? }; at least one field required
DELETE /api/deeds/:id → 204
Status values: planned | done
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: updateDeed and deleteDeed mutations</name>
  <files>frontend/src/store/deedsApi.ts</files>
  <read_first>frontend/src/store/deedsApi.ts, .planning/phases/02-my-deeds/02-PATTERNS.md</read_first>
  <acceptance_criteria>
    - updateDeed mutation PATCH deeds/:id with { id, body }
    - deleteDeed mutation DELETE deeds/:id
    - Both invalidateTags Deed LIST
    - Export useUpdateDeedMutation, useDeleteDeedMutation
  </acceptance_criteria>
  <action>
    Add mutations to existing deedsApi file from Plans 01–02.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q "updateDeed" frontend/src/store/deedsApi.ts && rg -q "deleteDeed" frontend/src/store/deedsApi.ts</automated>
  </verify>
  <done>PATCH and DELETE endpoints available to components</done>
</task>

<task type="auto">
  <name>Task 2: DeedCard actions, inline edit, DeleteDeedModal</name>
  <files>frontend/src/components/deeds/DeedCard.tsx, frontend/src/components/deeds/DeleteDeedModal.tsx, frontend/src/components/deeds/DeedList.tsx, frontend/src/app/(app)/deeds/page.tsx</files>
  <read_first>.planning/phases/02-my-deeds/02-UI-SPEC.md, frontend/src/components/ui/TextButton.tsx, frontend/src/components/ui/PrimaryButton.tsx</read_first>
  <acceptance_criteria>
    - View mode action row: Edit, Mark done OR Mark planned, Delete (destructive TextButton styling)
    - Edit mode: title + description fields, Save changes / Saving…, Cancel
    - Mark done: PATCH { status: 'done' }; button disabled + "Marking done…" while pending
    - Mark planned: PATCH { status: 'planned' }; "Marking planned…" while pending
    - No optimistic UI — card stays in section until refetch completes
    - Delete opens DeleteDeedModal: title "Delete deed?", body "This can't be undone.", primary "Delete deed" (red), secondary "Keep deed"
    - Modal: role=dialog, aria-modal, Escape closes without delete
    - On delete 204: close modal, card gone (no toast)
    - Edit on both planned and done cards
    - 400 on edit maps via mapDeedValidationErrors
  </acceptance_criteria>
  <action>
    DeedList passes callbacks or DeedCard owns modal state with deed id. Only one card in edit mode at a time (local state on page or card). DeleteDeedModal is presentational + mutation trigger. Overlay click does NOT dismiss (UI-SPEC).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
    <automated>rg -q "Delete deed" frontend/src/components/deeds && rg -q "Keep deed" frontend/src/components/deeds</automated>
  </verify>
  <done>Full edit/status/delete flows work against running API</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-04 | Denial of Service | Accidental delete | mitigate | Modal confirmation (D-16) |
| T-02-05 | Tampering | DELETE by id | mitigate | Backend owner check; UI only sends own deed ids from list |
</threat_model>

<verification>
- Manual E2E on /deeds: create (Plan 02) → edit title → mark done → mark planned → delete with modal
- `cd frontend && npm run build && npm run lint`
</verification>

<success_criteria>
DEED-03 and DEED-04 satisfied; Phase 2 ROADMAP success criteria achievable.
</success_criteria>

<output>
Create `.planning/phases/02-my-deeds/02-03-SUMMARY.md` when done
</output>

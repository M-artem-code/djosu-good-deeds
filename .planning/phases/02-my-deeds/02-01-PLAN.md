---
phase: 02-my-deeds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/types/deed.ts
  - frontend/src/store/baseApi.ts
  - frontend/src/store/deedsApi.ts
  - frontend/src/components/deeds/StatusBadge.tsx
  - frontend/src/components/deeds/DeedCard.tsx
  - frontend/src/components/deeds/DeedList.tsx
  - frontend/src/app/(app)/deeds/page.tsx
autonomous: true
requirements:
  - DEED-01
must_haves:
  truths:
    - "D-01: Deeds render as cards with rounded-xl border shadow-sm"
    - "D-02: List split into Planned and Done sections"
    - "D-03: Status badge Planned or Done on each card"
    - "D-04: Order matches API (newest createdAt first)"
    - "D-05: Description shows line-clamp-2 snippet when present"
    - "D-20: Initial fetch shows centered Loading…"
    - "D-21: Zero deeds shows No deeds yet empty state"
    - "D-22: Hide section when that status has zero items"
    - "User sees their deeds with planned/done status on /deeds"
  artifacts:
    - path: frontend/src/store/deedsApi.ts
      provides: getDeeds query with Deed LIST tag
      contains: getDeeds
    - path: frontend/src/components/deeds/DeedList.tsx
      provides: Planned/Done sections
      contains: Planned
    - path: frontend/src/app/(app)/deeds/page.tsx
      provides: Replaced stub with live list
      contains: My Deeds
  key_links:
    - from: frontend/src/app/(app)/deeds/page.tsx
      to: GET /api/deeds
      via: useGetDeedsQuery
      pattern: useGetDeedsQuery
---

## Phase Goal

**As a** signed-in user, **I want to** see my deeds grouped by status, **so that** I know what is planned versus done before adding or editing.

<objective>
Deliver read path: deed types, RTK Query `getDeeds`, view-only deed cards in Planned/Done sections, loading and empty states per UI-SPEC.

Purpose: Satisfy DEED-01 and establish list foundation for create/edit/delete plans.
Output: Live `/deeds` list replacing Phase 1 stub; cards without action buttons yet (actions in Plan 03).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-my-deeds/02-CONTEXT.md
@.planning/phases/02-my-deeds/02-UI-SPEC.md
@.planning/phases/02-my-deeds/02-PATTERNS.md
@.planning/phases/01-app-shell-authentication/01-PATTERNS.md
@backend/src/deeds/dto/deed-public.dto.ts
@backend/src/deeds/deeds.controller.ts
@backend/test/deeds.e2e-spec.ts
@frontend/src/store/authApi.ts
@frontend/src/components/auth/AuthCard.tsx

<interfaces>
DeedPublic: { _id, ownerId, title, description?, status: 'planned'|'done', createdAt, updatedAt }
GET /api/deeds → 200 DeedPublic[]
Default status on create is planned (backend) — not used in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deed types and deedsApi list endpoint</name>
  <files>frontend/src/lib/types/deed.ts, frontend/src/store/baseApi.ts, frontend/src/store/deedsApi.ts</files>
  <read_first>frontend/src/store/authApi.ts, frontend/src/store/baseApi.ts, backend/src/deeds/dto/deed-public.dto.ts, .planning/phases/02-my-deeds/02-PATTERNS.md</read_first>
  <acceptance_criteria>
    - DeedPublic and DeedStatus mirror backend DTO
    - baseApi tagTypes includes "Deed" (keep "User")
    - deedsApi exports getDeeds query and useGetDeedsQuery
    - providesTags: [{ type: "Deed", id: "LIST" }]
    - deedsApi registered via injectEndpoints (import deedsApi in store/index.ts if required for endpoint registration)
  </acceptance_criteria>
  <action>
    Add types file. Extend baseApi tagTypes. Create deedsApi with getDeeds only in this task (mutations deferred to Plans 02–03). Follow authApi inject pattern exactly.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q "getDeeds" frontend/src/store/deedsApi.ts && rg -q '"Deed"' frontend/src/store/baseApi.ts</automated>
  </verify>
  <done>getDeeds compiles and is importable from deeds page</done>
</task>

<task type="auto">
  <name>Task 2: DeedList, StatusBadge, view-only DeedCard, deeds page</name>
  <files>frontend/src/components/deeds/StatusBadge.tsx, frontend/src/components/deeds/DeedCard.tsx, frontend/src/components/deeds/DeedList.tsx, frontend/src/app/(app)/deeds/page.tsx</files>
  <read_first>.planning/phases/02-my-deeds/02-UI-SPEC.md, frontend/src/app/(app)/deeds/page.tsx, frontend/src/components/auth/AuthCard.tsx</read_first>
  <acceptance_criteria>
    - Page heading "My Deeds" per UI-SPEC
    - useGetDeedsQuery: isLoading → centered "Loading…" (zinc-500)
    - Empty array → heading "No deeds yet" + body "Add your first good deed below." (add form placeholder OK — Plan 02)
    - DeedList renders Planned then Done sections; omit section when empty
    - DeedCard view mode: title semibold, StatusBadge, optional line-clamp-2 description
    - Card surface: rounded-xl border bg-white shadow-sm dark variants per UI-SPEC
    - No Edit/Mark/Delete buttons in this plan (view-only)
    - Page is "use client"
  </acceptance_criteria>
  <action>
    Implement StatusBadge with Planned/Done copy and semantic colors from UI-SPEC. DeedCard accepts deed prop and mode="view" only for now. Wire page under existing (app) layout (AppNav + ErrorBanner inherited). Remove stub paragraph text.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
    <automated>rg -q "No deeds yet" frontend/src && rg -q "Loading" frontend/src/app/(app)/deeds/page.tsx</automated>
  </verify>
  <done>Logged-in user with backend deeds sees sectioned list; empty and loading states match UI-SPEC</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-01 | Information Disclosure | Deed list in DOM | accept | Owner-only API; no friends data in Phase 2 |
| T-02-02 | Spoofing | GET /api/deeds | mitigate | Bearer via baseApi; 401 handled by Phase 1 |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint`
- Manual: login → /deeds shows list or empty state; network tab shows GET /api/deeds with Bearer
</verification>

<success_criteria>
DEED-01 satisfied: user sees deeds with planned/done status in UI sections.
</success_criteria>

<output>
Create `.planning/phases/02-my-deeds/02-01-SUMMARY.md` when done
</output>

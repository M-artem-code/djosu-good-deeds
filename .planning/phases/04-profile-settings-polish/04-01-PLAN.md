---
phase: 04-profile-settings-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/store/usersApi.ts
  - frontend/src/components/settings/ProfileSettingsCard.tsx
  - frontend/src/app/(app)/settings/page.tsx
autonomous: true
requirements:
  - PROF-01
  - PROF-02
must_haves:
  truths:
    - "D-01: Single scrollable /settings page — profile card top, delete area reserved at bottom (wired in Plan 02)"
    - "D-02: Email shown as label + plain text row, not disabled TextField"
    - "D-03: Page heading Settings only — no @tag or displayName subtitle"
    - "D-04: Profile fields inside rounded border card matching DeedCard surfaces"
    - "D-05: displayName and tag always editable on load — no Edit profile toggle"
    - "D-06: One Save changes button; PATCH sends only changed fields; disabled when pristine or in-flight"
    - "D-07: No tag-change confirmation modal"
    - "D-08: normalizeTag before submit (strip @, lowercase)"
    - "D-09: 400 inline under fields; 409 under tag; non-field errors via ErrorBanner"
    - "PROF-01: User views email, displayName, tag on settings"
    - "PROF-02: User updates displayName/tag with 400/409 shown inline"
    - "AppNav @tag updates after successful tag PATCH (authSlice.setUser)"
  artifacts:
    - path: frontend/src/store/usersApi.ts
      provides: getMe query and updateMe mutation
      contains: getMe
    - path: frontend/src/components/settings/ProfileSettingsCard.tsx
      provides: Profile form with email row, fields, Save
      contains: Save changes
    - path: frontend/src/app/(app)/settings/page.tsx
      provides: Live settings page replacing stub
      contains: Settings
  key_links:
    - from: frontend/src/app/(app)/settings/page.tsx
      to: GET /api/users/me
      via: useGetMeQuery
      pattern: useGetMeQuery
    - from: frontend/src/components/settings/ProfileSettingsCard.tsx
      to: PATCH /api/users/me
      via: useUpdateMeMutation
      pattern: updateMe
    - from: frontend/src/store/usersApi.ts
      to: authSlice.setUser
      via: dispatch on updateMe fulfilled
      pattern: setUser
---

## Phase Goal

**As a** signed-in user, **I want to** view and update my profile on the settings page, **so that** my display name and tag stay accurate across the app.

<objective>
Deliver profile view + edit vertical slice: usersApi getMe/updateMe, ProfileSettingsCard, live /settings page with auth sync after PATCH.

Purpose: Satisfy PROF-01 and PROF-02; establish settings shell for delete account (Plan 02).
Output: Functional settings profile block; delete control placeholder gap at bottom for Plan 02.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-profile-settings-polish/04-CONTEXT.md
@.planning/phases/04-profile-settings-polish/04-UI-SPEC.md
@.planning/phases/01-app-shell-authentication/01-PATTERNS.md
@.planning/phases/03-friends-shared-deeds/03-01-PLAN.md
@backend/src/users/users.controller.ts
@backend/src/users/dto/update-profile.dto.ts
@frontend/src/store/usersApi.ts
@frontend/src/store/authSlice.ts
@frontend/src/lib/api/validation-errors.ts
@frontend/src/lib/utils/normalize-tag.ts
@frontend/src/components/deeds/DeedCard.tsx
@frontend/src/components/friends/AddFriendForm.tsx
@frontend/src/app/(app)/settings/page.tsx

<interfaces>
UserPublic: { _id, email, displayName, tag, createdAt, updatedAt } — frontend/src/lib/types/user.ts
GET /api/users/me → 200 UserPublic
PATCH /api/users/me body: partial { displayName?, tag? } — at least one field required (backend assertAtLeastOneField)
PATCH success → 200 UserPublic; 400 validation; 409 tag conflict
normalizeTag: trim, lowercase, strip leading @ — frontend/src/lib/utils/normalize-tag.ts
mapValidationErrors / mapConflictError — frontend/src/lib/api/validation-errors.ts
authSlice.setUser(UserPublic) — sync header @tag after PATCH
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: usersApi getMe and updateMe endpoints</name>
  <files>frontend/src/store/usersApi.ts</files>
  <read_first>frontend/src/store/deedsApi.ts, frontend/src/lib/types/user.ts, backend/src/users/users.controller.ts</read_first>
  <acceptance_criteria>
    - getMe: query GET users/me, providesTags [{ type: User, id: ME }]
    - updateMe: mutation PATCH users/me with partial body, invalidatesTags [{ type: User, id: ME }]
    - Export useGetMeQuery, useUpdateMeMutation
    - Keep existing getUserByTag unchanged
    - Response typed as UserPublic
  </acceptance_criteria>
  <action>
    Extend usersApi injectEndpoints per Phase 1 RTK pattern. getMe runs on settings mount for fresh profile (D-05 discretion). updateMe accepts Partial with displayName and/or tag only — never send empty body. Do not add deleteMe in this plan (Plan 02).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q "getMe" frontend/src/store/usersApi.ts && rg -q "updateMe" frontend/src/store/usersApi.ts && rg -q "useGetMeQuery" frontend/src/store/usersApi.ts</automated>
  </verify>
  <done>getMe and updateMe registered with correct tags and hooks exported</done>
</task>

<task type="auto">
  <name>Task 2: ProfileSettingsCard — view email, edit fields, Save with validation</name>
  <files>frontend/src/components/settings/ProfileSettingsCard.tsx</files>
  <read_first>frontend/src/components/ui/TextField.tsx, frontend/src/components/deeds/DeedCard.tsx, .planning/phases/04-profile-settings-polish/04-UI-SPEC.md, frontend/src/components/friends/AddFriendForm.tsx</read_first>
  <acceptance_criteria>
    - Card: rounded-xl border bg-white p-4 dark tokens per D-04 / UI-SPEC
    - Email row: label Email (text-sm zinc-500) + plain text value (text-base) per D-02 — not TextField
    - displayName and tag TextFields always visible per D-05
    - Save changes PrimaryButton; idle/loading copy per UI-SPEC (Saving…)
    - Disabled when no field differs from initial user OR mutation isLoading per D-06
    - On submit: build PATCH body with only changed keys; normalizeTag(tag) before compare/submit per D-08
    - 400: mapValidationErrors on displayName/tag; 409: mapConflictError → tag inline per D-09
    - Non-field/5xx: local formError + ErrorBanner pattern from login (do not rely on baseApi banner for 400/409 on this form)
    - On success: dispatch setUser(updatedUser) so AppNav @tag updates per CONTEXT discretion
    - No modal on tag change per D-07
  </acceptance_criteria>
  <action>
    Create client component accepting initial UserPublic from parent (from useGetMeQuery). Initialize local state from props; track dirty by comparing normalized tag and trimmed displayName to source. Implement handleSubmit with useUpdateMeMutation.unwrap(), setUser on success, reset dirty baseline from response. Mirror AddFriendForm error extraction for status 400/409. Props: user: UserPublic, onUpdated optional callback if page needs refetch — prefer setUser only.
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
    <automated>rg -q "Save changes" frontend/src/components/settings/ProfileSettingsCard.tsx && rg -v "disabled.*TextField|TextField.*disabled" frontend/src/components/settings/ProfileSettingsCard.tsx | rg -q "Email" || rg -q "Email" frontend/src/components/settings/ProfileSettingsCard.tsx</automated>
  </verify>
  <done>Profile card renders email read-only, editable fields, Save with partial PATCH and inline errors</done>
</task>

<task type="auto">
  <name>Task 3: Settings page vertical slice — load, profile card, bottom gap for delete</name>
  <files>frontend/src/app/(app)/settings/page.tsx</files>
  <read_first>frontend/src/app/(app)/deeds/page.tsx, .planning/phases/04-profile-settings-polish/04-CONTEXT.md</read_first>
  <acceptance_criteria>
    - use client page; title Settings only at text-xl per D-03
    - useGetMeQuery: show centered loading or in-card skeleton until data; isError → Couldn't load profile + Try again refetch (profile fetch failure distinct from list polish in Plan 03)
    - On success: render ProfileSettingsCard with data
    - Layout: flex flex-col gap-8; profile card; gap-8 spacer before bottom where Plan 02 adds Delete account TextButton per D-01/D-12
    - Replace stub paragraph entirely
    - No delete UI in this plan — only structural gap-8 at page bottom
  </acceptance_criteria>
  <action>
    Replace settings/page.tsx stub with live page per D-01 single-page layout. Wire useGetMeQuery and pass user to ProfileSettingsCard. Keep delete-account zone empty at bottom (comment Delete account — Plan 02 acceptable). Do not add tabs or secondary routes.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q "useGetMeQuery" frontend/src/app/\(app\)/settings/page.tsx && rg -q "ProfileSettingsCard" frontend/src/app/\(app\)/settings/page.tsx && rg -v "available here soon" frontend/src/app/\(app\)/settings/page.tsx</automated>
  </verify>
  <done>Authenticated user opens /settings, sees profile fields, can save changes; header tag updates after tag edit</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Bearer JWT on GET/PATCH /api/users/me; response includes email (owner only) |
| client→UI | Email shown only on /settings; never on public friend surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | ProfileSettingsCard | mitigate | Render email only on settings; UserPublic not logged to console |
| T-04-02 | Tampering | PATCH body | mitigate | Send only changed fields; normalizeTag before submit; backend ValidationPipe enforces IsTag |
| T-04-03 | Spoofing | updateMe | transfer | Bearer via baseQueryWithReauth; 401 clears session |
| T-04-SC | Tampering | npm installs | accept | No new packages this plan |
</threat_model>

<verification>
- npm run build and npm run lint pass in frontend
- /settings shows email (read-only), editable displayName/tag, Save changes
- Save disabled when unchanged; PATCH with tag change updates AppNav @tag
- 400/409 from API show inline under fields, not generic empty success
</verification>

<success_criteria>
- PROF-01 and PROF-02 satisfied
- D-01 through D-09 implemented on settings profile block
- Ready for Plan 02 delete account on same page
</success_criteria>

<output>
Create `.planning/phases/04-profile-settings-polish/04-01-SUMMARY.md` when done
</output>

---
phase: 04-profile-settings-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - frontend/src/store/usersApi.ts
  - frontend/src/components/settings/DeleteAccountModal.tsx
  - frontend/src/app/(app)/settings/page.tsx
  - frontend/src/app/(auth)/login/LoginPageContent.tsx
autonomous: true
requirements:
  - PROF-03
must_haves:
  truths:
    - "D-10: Delete confirmation is simple modal only — no type-tag or checkbox gate"
    - "D-11: Modal title Delete account?; body This can't be undone.; primary Delete account; secondary Keep account"
    - "D-12: Red TextButton Delete account at page bottom below profile card with gap-8 separation"
    - "D-13: On 204 clearSession and redirect /login?reason=account_deleted with dismissible info banner"
    - "PROF-03: User deletes account with confirmation; post-204 redirected out of app"
  artifacts:
    - path: frontend/src/store/usersApi.ts
      provides: deleteMe mutation
      contains: deleteMe
    - path: frontend/src/components/settings/DeleteAccountModal.tsx
      provides: Confirmation modal mirroring DeleteDeedModal
      contains: Delete account?
    - path: frontend/src/app/(app)/settings/page.tsx
      provides: Delete trigger wired to modal
      contains: Delete account
    - path: frontend/src/app/(auth)/login/LoginPageContent.tsx
      provides: Post-delete banner on login
      contains: account_deleted
  key_links:
    - from: frontend/src/components/settings/DeleteAccountModal.tsx
      to: DELETE /api/users/me
      via: useDeleteMeMutation
      pattern: deleteMe
    - from: frontend/src/components/settings/DeleteAccountModal.tsx
      to: authSlice.clearSession
      via: dispatch before router.replace
      pattern: clearSession
    - from: frontend/src/app/(auth)/login/LoginPageContent.tsx
      to: searchParams reason
      via: account_deleted banner
      pattern: account_deleted
---

## Phase Goal

**As a** signed-in user, **I want to** delete my account with a clear confirmation, **so that** I can leave the service and my session is fully cleared.

<objective>
Deliver delete-account vertical slice: deleteMe mutation, DeleteAccountModal, settings trigger, login redirect banner.

Purpose: Satisfy PROF-03; complete settings page danger flow per locked decisions D-10–D-13.
Output: End-to-end account deletion from /settings through 204 to login with user-visible confirmation.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-profile-settings-polish/04-CONTEXT.md
@.planning/phases/04-profile-settings-polish/04-UI-SPEC.md
@.planning/phases/04-profile-settings-polish/04-01-PLAN.md
@frontend/src/components/deeds/DeleteDeedModal.tsx
@frontend/src/store/usersApi.ts
@frontend/src/store/authSlice.ts
@frontend/src/app/(app)/settings/page.tsx
@frontend/src/app/(auth)/login/LoginPageContent.tsx
@backend/src/users/users.controller.ts

<interfaces>
DELETE /api/users/me → 204 No Content (no body)
clearSession() — clears token + auth.user
DeleteDeedModal pattern: role=dialog, Escape closes, initial focus on secondary (Keep account per UI-SPEC a11y)
Login reason query: session_expired existing; add account_deleted banner copy per UI-SPEC
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: usersApi deleteMe mutation</name>
  <files>frontend/src/store/usersApi.ts</files>
  <read_first>frontend/src/store/deedsApi.ts deleteDeed mutation, backend/src/users/users.controller.ts</read_first>
  <acceptance_criteria>
    - deleteMe: mutation DELETE users/me, void response
    - invalidatesTags or resetApiState not required — session cleared client-side on success
    - Export useDeleteMeMutation
    - Do not trigger spurious ErrorBanner on successful 204 (unwrap in modal handles flow)
  </acceptance_criteria>
  <action>
    Add deleteMe to existing usersApi injectEndpoints. Query url users/me method DELETE. Typing void | undefined for 204 empty body. Keep getMe/updateMe/getUserByTag from Plan 01 intact.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q "deleteMe" frontend/src/store/usersApi.ts && rg -q "useDeleteMeMutation" frontend/src/store/usersApi.ts</automated>
  </verify>
  <done>deleteMe mutation registered and exported</done>
</task>

<task type="auto">
  <name>Task 2: DeleteAccountModal — brief confirm, 204 cleanup, redirect</name>
  <files>frontend/src/components/settings/DeleteAccountModal.tsx</files>
  <read_first>frontend/src/components/deeds/DeleteDeedModal.tsx, .planning/phases/04-profile-settings-polish/04-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - Mirror DeleteDeedModal structure: fixed overlay, max-w-[400px], dialog a11y attributes per UI-SPEC
    - Copy exactly per D-11/UI-SPEC: title Delete account?; body This can't be undone.; confirm Delete account / Deleting…; cancel Keep account
    - No typed confirmation, no checkbox per D-10
    - Initial focus on Keep account; Escape closes when not loading
    - On confirm: deleteMe.unwrap() → dispatch clearSession → router.replace(/login?reason=account_deleted) per D-13
    - Use useRouter from next/navigation; useAppDispatch for clearSession
    - Errors on failure: rely on baseApi banner or local message; do not clear session unless 204
  </acceptance_criteria>
  <action>
    Create DeleteAccountModal with onClose prop. Destructive red filled button for confirm (same classes as DeleteDeedModal). Do not show deed title — no sr-only extra content needed beyond modal copy. Parent owns open state.
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
    <automated>rg -q "Delete account\?" frontend/src/components/settings/DeleteAccountModal.tsx && rg -q "Keep account" frontend/src/components/settings/DeleteAccountModal.tsx && rg -q "clearSession" frontend/src/components/settings/DeleteAccountModal.tsx</automated>
  </verify>
  <done>Modal confirms delete, clears session, redirects to login with reason query on 204</done>
</task>

<task type="auto">
  <name>Task 3: Wire delete trigger on settings + login account_deleted banner</name>
  <files>frontend/src/app/(app)/settings/page.tsx, frontend/src/app/(auth)/login/LoginPageContent.tsx</files>
  <read_first>frontend/src/app/(app)/settings/page.tsx, frontend/src/app/(auth)/login/LoginPageContent.tsx, .planning/phases/04-profile-settings-polish/04-CONTEXT.md</read_first>
  <acceptance_criteria>
    - Settings: red TextButton Delete account below profile card with gap-8 per D-12
    - useState for modal open; render DeleteAccountModal when open
    - Page keeps profile block from Plan 01 above delete control per D-01
    - Login: when reason=account_deleted show dismissible ErrorBanner (info-style message e.g. Your account was deleted.) mirroring session_expired pattern per D-13/UI-SPEC
    - Forbidden labels: no generic OK/Cancel on modal
  </acceptance_criteria>
  <action>
    Extend settings/page.tsx: add TextButton with text-red-600 classes per UI-SPEC below ProfileSettingsCard. Wire modal open/close. Update LoginPageContent: add accountDeleted flag from searchParams, separate dismissed state, banner above AuthCard. Use router.replace not push on delete so back button does not return to settings.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q "Delete account" frontend/src/app/\(app\)/settings/page.tsx && rg -q "DeleteAccountModal" frontend/src/app/\(app\)/settings/page.tsx && rg -q "account_deleted" frontend/src/app/\(auth\)/login/LoginPageContent.tsx</automated>
  </verify>
  <done>User can delete account from settings; lands on login with visible deleted-account message; cannot access protected routes without new login</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | DELETE /api/users/me requires valid JWT; irreversible server-side cascade |
| client→session | clearSession must run on 204 before redirect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Elevation of Privilege | deleteMe | mitigate | Mutation only callable when authenticated; 401 handled by baseApi |
| T-04-05 | Denial of Service | Accidental delete | mitigate | Modal confirmation required per D-10; destructive styling |
| T-04-06 | Repudiation | Post-delete session | mitigate | clearSession removes token; redirect to public login |
| T-04-SC | Tampering | npm installs | accept | No new packages this plan |
</threat_model>

<verification>
- npm run build and npm run lint pass
- /settings shows Delete account TextButton below profile card
- Modal copy matches UI-SPEC; Keep account closes without API call
- Successful delete → login with banner; protected routes inaccessible
</verification>

<success_criteria>
- PROF-03 satisfied
- D-10 through D-13 implemented
- Settings page complete (profile + delete)
</success_criteria>

<output>
Create `.planning/phases/04-profile-settings-polish/04-02-SUMMARY.md` when done
</output>

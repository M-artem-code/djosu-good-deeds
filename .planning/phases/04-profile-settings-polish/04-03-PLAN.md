---
phase: 04-profile-settings-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
files_modified:
  - frontend/src/components/ui/DeedSkeletonCard.tsx
  - frontend/src/components/ui/FriendSkeletonCard.tsx
  - frontend/src/app/(app)/deeds/page.tsx
  - frontend/src/app/(app)/friends/page.tsx
autonomous: true
requirements:
  - UX-04
must_haves:
  truths:
    - "D-14: /deeds and /friends loading use skeleton card rows (pulse), not centered Loading…"
    - "D-15: /friends/[tag] unchanged — no skeleton/empty/error polish this phase"
    - "D-16: Empty state copy polish on deeds and friends only — headings/helpers per UI-SPEC, no icons"
    - "D-17: isError shows explicit error + Try again refetch — never empty state on fetch failure"
    - "UX-04: List pages show loading indicators and helpful empty states"
  artifacts:
    - path: frontend/src/components/ui/DeedSkeletonCard.tsx
      provides: Deed list skeleton placeholder
      contains: animate-pulse
    - path: frontend/src/components/ui/FriendSkeletonCard.tsx
      provides: Friend list skeleton placeholder
      contains: animate-pulse
    - path: frontend/src/app/(app)/deeds/page.tsx
      provides: Skeleton load, error+retry, polished empty copy
      contains: Couldn't load your deeds
    - path: frontend/src/app/(app)/friends/page.tsx
      provides: Skeleton load, error+retry, polished empty copy
      contains: Try again
  key_links:
    - from: frontend/src/app/(app)/deeds/page.tsx
      to: useGetDeedsQuery refetch
      via: TextButton Try again
      pattern: refetch
    - from: frontend/src/app/(app)/friends/page.tsx
      to: useGetFriendsQuery refetch
      via: TextButton Try again
      pattern: refetch
---

## Phase Goal

**As a** signed-in user, **I want to** see clear loading, empty, and error states on my deeds and friends lists, **so that** I know whether data is loading, absent, or failed to load.

<objective>
Deliver UX-04 polish vertical slice: skeleton cards on /deeds and /friends, refined empty copy, fetch-error with retry. Explicitly exclude /friends/[tag] per D-15.

Purpose: Satisfy UX-04 and close Phase 2 review gap (error vs empty conflation).
Output: Polished list UX on two main list routes only.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-profile-settings-polish/04-CONTEXT.md
@.planning/phases/04-profile-settings-polish/04-UI-SPEC.md
@.planning/phases/02-my-deeds/02-REVIEW.md
@frontend/src/app/(app)/deeds/page.tsx
@frontend/src/app/(app)/friends/page.tsx
@frontend/src/components/deeds/DeedCard.tsx
@frontend/src/components/friends/FriendCard.tsx
@frontend/src/app/(app)/friends/[tag]/page.tsx

<interfaces>
useGetDeedsQuery / useGetFriendsQuery return { isLoading, isError, refetch, data }
Skeleton: 3 rows, gap-4, aria-busy=true on list container per UI-SPEC
Error copy: deeds Couldn't load your deeds.; friends Couldn't load friends.
Empty copy: deeds No deeds yet / Add your first good deed below.; friends No friends yet / Add someone by tag to see their deeds.
/friends/[tag]/page.tsx — do not modify (D-15)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeedSkeletonCard and FriendSkeletonCard components</name>
  <files>frontend/src/components/ui/DeedSkeletonCard.tsx, frontend/src/components/ui/FriendSkeletonCard.tsx</files>
  <read_first>frontend/src/components/deeds/DeedCard.tsx, frontend/src/components/friends/FriendCard.tsx, .planning/phases/04-profile-settings-polish/04-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - DeedSkeletonCard: rounded-xl border p-4; pulse bars for title (h-5 w-3/4) and optional description (h-4 w-full) per UI-SPEC
    - FriendSkeletonCard: rounded-xl border p-4; pulse bars for tag (h-5 w-1/3) and displayName (h-4 w-1/2)
    - zinc-200 / zinc-700 animate-pulse blocks
    - No data fetching — presentational only
    - Export named components for page import
  </acceptance_criteria>
  <action>
    Create two small skeleton components matching real card dimensions (discretion: separate files vs one SkeletonCard — use two files for layout fidelity per D-14). Min height similar to real cards (~56px+). No icons in skeletons.
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
    <automated>rg -q "animate-pulse" frontend/src/components/ui/DeedSkeletonCard.tsx && rg -q "animate-pulse" frontend/src/components/ui/FriendSkeletonCard.tsx</automated>
  </verify>
  <done>Skeleton card components ready for list pages</done>
</task>

<task type="auto">
  <name>Task 2: Deeds page — skeleton loading, error retry, empty copy</name>
  <files>frontend/src/app/(app)/deeds/page.tsx</files>
  <read_first>frontend/src/app/(app)/deeds/page.tsx, frontend/src/components/deeds/DeedList.tsx, .planning/phases/04-profile-settings-polish/04-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - isLoading: page title visible; stack of 3 DeedSkeletonCard with gap-4 and aria-busy=true on container per D-14
    - isError: semibold heading Couldn't load your deeds. + helper + TextButton Try again calling refetch per D-17/UI-SPEC — NOT empty state, NOT AddDeedForm
    - Success empty: heading No deeds yet; helper Add your first good deed below. per D-16
    - Success with data: unchanged list behavior; AddDeedForm + DeedList
    - Remove centered Loading… paragraph
  </acceptance_criteria>
  <action>
    Refactor deeds/page.tsx loading branch to skeleton stack. Destructure refetch from useGetDeedsQuery. Error branch: centered flex column with Try again TextButton. Align empty copy exactly to UI-SPEC (fix Could not vs Couldn't if mismatched). Do not touch /friends/[tag] or settings.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q "DeedSkeletonCard" frontend/src/app/\(app\)/deeds/page.tsx && rg -q "refetch" frontend/src/app/\(app\)/deeds/page.tsx && ! rg -q "Loading…" frontend/src/app/\(app\)/deeds/page.tsx</automated>
    <automated>rg -q "Couldn't load your deeds" frontend/src/app/\(app\)/deeds/page.tsx</automated>
  </verify>
  <done>/deeds shows skeletons while loading, retry on error, polished empty copy when truly empty</done>
</task>

<task type="auto">
  <name>Task 3: Friends page — skeleton loading, error retry, empty copy</name>
  <files>frontend/src/app/(app)/friends/page.tsx, frontend/src/components/friends/FriendsList.tsx</files>
  <read_first>frontend/src/app/(app)/friends/page.tsx, frontend/src/components/friends/FriendsList.tsx, .planning/phases/04-profile-settings-polish/04-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - isLoading: Friends title (28px) visible; 3 FriendSkeletonCard stack per D-14
    - isError: Couldn't load friends. + Try again refetch per D-17 — not FriendsList empty state
    - FriendsList empty copy already No friends yet — verify helper Add someone by tag to see their deeds. per D-16 (update FriendsList if wording differs)
    - Intro blurb and AddFriendForm only render after successful fetch (not during load/error)
    - Do not modify friends/[tag]/page.tsx per D-15
  </acceptance_criteria>
  <action>
    Update friends/page.tsx: skeleton loading, refetch on error with Try again. If FriendsList empty helper text differs from UI-SPEC, update FriendsList.tsx in this task (only copy strings). Keep Phase 3 intro and add/remove flows intact. Confirm grep shows no changes to friends/[tag]/page.tsx.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q "FriendSkeletonCard" frontend/src/app/\(app\)/friends/page.tsx && rg -q "Try again" frontend/src/app/\(app\)/friends/page.tsx</automated>
    <automated>test -z "$(git diff --name-only -- 'frontend/src/app/(app)/friends/[tag]/page.tsx' 2>/dev/null)"</automated>
  </verify>
  <done>/friends shows skeletons, retry on error, polished empty; friend deeds route untouched</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→UI | Error vs empty distinction prevents misleading UX (no phantom empty lists) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | Error messages | accept | Generic Couldn't load copy; no stack traces in UI |
| T-04-08 | Tampering | refetch | mitigate | RTK Query refetch uses same authenticated baseQuery |
| T-04-SC | Tampering | npm installs | accept | No new packages this plan |
</threat_model>

<verification>
- npm run build and npm run lint pass
- /deeds and /friends show 3 skeleton cards while loading (not Loading… text)
- Simulated API failure shows error + Try again, not empty list or add forms
- /friends/[tag] file unchanged from Phase 3
- Empty states use UI-SPEC copy without icons
</verification>

<success_criteria>
- UX-04 satisfied on /deeds and /friends
- D-14, D-16, D-17 implemented; D-15 respected (no [tag] page changes)
- Phase 4 list polish complete
</success_criteria>

<output>
Create `.planning/phases/04-profile-settings-polish/04-03-SUMMARY.md` when done
</output>

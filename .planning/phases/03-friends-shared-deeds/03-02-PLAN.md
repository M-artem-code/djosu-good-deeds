---
phase: 03-friends-shared-deeds
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - frontend/src/lib/api/validation-errors.ts
  - frontend/src/store/friendsApi.ts
  - frontend/src/components/friends/AddFriendForm.tsx
  - frontend/src/components/friends/FriendCard.tsx
  - frontend/src/components/friends/RemoveFriendModal.tsx
  - frontend/src/components/friends/BlockIncomingSection.tsx
  - frontend/src/components/friends/BlockIncomingModal.tsx
  - frontend/src/app/(app)/friends/page.tsx
autonomous: true
requirements:
  - FRND-02
  - FRND-03
  - FRND-04
must_haves:
  truths:
    - "D-04: Add and block forms normalize tag before submit; inline field error for 400 self-add"
    - "D-07: Inline add form collapsed by default; Add friend reveals tag field"
    - "D-08: Remove outgoing friendship requires confirmation modal"
    - "D-09: Separate Block someone who added you section below friends list"
    - "D-10: Block action uses confirmation modal before submit"
    - "D-11: User-facing verb Block — not Revoke or Remove follower"
    - "D-12: Revoke 404 shows generic inline No one with that tag has added you"
    - "D-20: Add friend 409 Already friends shows inline under tag field"
    - "D-21: Add friend 404 User not found shows inline under tag field"
    - "User can add friend by tag; duplicate shows 409 with clear message"
    - "User can remove outgoing friendship from the list"
    - "User can revoke incoming friendship by tag via block flow"
  artifacts:
    - path: frontend/src/components/friends/AddFriendForm.tsx
      provides: Collapsed add-by-tag form
      contains: Add friend
    - path: frontend/src/components/friends/RemoveFriendModal.tsx
      provides: Remove friend confirmation per D-08
      contains: Keep friend
    - path: frontend/src/components/friends/BlockIncomingSection.tsx
      provides: Block-by-tag section per D-09
      contains: Block someone who added you
    - path: frontend/src/store/friendsApi.ts
      provides: addFriend, removeFriend, revokeIncoming mutations
      contains: addFriend
  key_links:
    - from: frontend/src/components/friends/AddFriendForm.tsx
      to: POST /api/friends
      via: useAddFriendMutation
      pattern: addFriend
    - from: frontend/src/components/friends/RemoveFriendModal.tsx
      to: DELETE /api/friends/:friendshipId
      via: useRemoveFriendMutation
      pattern: removeFriend
    - from: frontend/src/components/friends/BlockIncomingModal.tsx
      to: DELETE /api/friends/incoming/:tag
      via: useRevokeIncomingMutation
      pattern: revokeIncoming
---

## Phase Goal

**As a** user building my network, **I want to** add friends by tag, remove people I follow, and block incoming access, **so that** I control who I see and who can see my deeds.

<objective>
Add collapsible AddFriendForm, remove-friend and block-incoming modals, friendsApi mutations, and inline error mapping for add/block forms.

Purpose: Satisfy FRND-02, FRND-03, FRND-04 with destructive confirmations per D-08 and D-10.
Output: Full friends management on `/friends` except viewing friend deeds (Plan 03).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-friends-shared-deeds/03-CONTEXT.md
@.planning/phases/03-friends-shared-deeds/03-UI-SPEC.md
@.planning/phases/03-friends-shared-deeds/03-PATTERNS.md
@backend/src/friends/friends.controller.ts
@backend/test/friends.e2e-spec.ts
@frontend/src/components/deeds/AddDeedForm.tsx
@frontend/src/components/deeds/DeleteDeedModal.tsx
@frontend/src/lib/api/validation-errors.ts
@frontend/src/app/(app)/register/page.tsx

<interfaces>
POST /api/friends body { tag } → 201 FriendItem; 400 self; 404 User not found; 409 Already friends
DELETE /api/friends/:friendshipId → 204
DELETE /api/friends/incoming/:tag → 204; 404 Friendship not found (show D-12 generic inline)
AddFriendBody: { tag: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: mapFriendTagValidationErrors and friendsApi mutations</name>
  <files>frontend/src/lib/api/validation-errors.ts, frontend/src/store/friendsApi.ts</files>
  <read_first>frontend/src/lib/api/validation-errors.ts, frontend/src/store/friendsApi.ts, .planning/phases/03-friends-shared-deeds/03-PATTERNS.md</read_first>
  <acceptance_criteria>
    - mapFriendTagValidationErrors maps Nest 400 message[] to { tag?: string } for self-add and tag validation per D-04
    - addFriend POST friends invalidatesTags Friend LIST
    - removeFriend DELETE friends/:friendshipId invalidatesTags Friend LIST
    - revokeIncoming DELETE friends/incoming/:tag with encodeURIComponent(tag) invalidatesTags Friend LIST
    - Export useAddFriendMutation, useRemoveFriendMutation, useRevokeIncomingMutation
    - Form 400/404/409 errors stay inline — do not dispatch global banner for these statuses
  </acceptance_criteria>
  <action>
    Extend validation-errors.ts with mapFriendTagValidationErrors without breaking existing auth/deed mappers. Add three mutations to friendsApi mirroring deedsApi inject pattern. Use normalizeTag on mutation args where body/route expects normalized tag. 404 on add shows API message inline per D-21; 409 shows API message per D-20; revoke 404 uses fixed D-12 copy in component not API text.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q "addFriend|removeFriend|revokeIncoming" frontend/src/store/friendsApi.ts && rg -q "mapFriendTagValidationErrors" frontend/src/lib/api/validation-errors.ts</automated>
  </verify>
  <done>All friend write mutations registered with LIST cache invalidation</done>
</task>

<task type="auto">
  <name>Task 2: AddFriendForm with collapsed reveal and inline errors</name>
  <files>frontend/src/components/friends/AddFriendForm.tsx, frontend/src/app/(app)/friends/page.tsx</files>
  <read_first>frontend/src/components/deeds/AddDeedForm.tsx, .planning/phases/03-friends-shared-deeds/03-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - Collapsed by default; TextButton Add friend reveals form per D-07
    - Auto-expand when friendCount === 0 per D-18 discretion (mirror AddDeedForm deedCount pattern)
    - Hint line near field: Enter their tag — no @ required per UI-SPEC
    - Submit normalizes tag via normalizeTag before addFriend per D-04
    - 400 → mapFriendTagValidationErrors inline under tag
    - 404 → inline tag error with API message (User not found) per D-21
    - 409 → inline tag error with API message (Already friends) per D-20
    - Success: clear field, collapse form, list refreshes via invalidatesTags
    - Button copy Add friend / Adding… per UI-SPEC; no generic Submit
    - Page wires AddFriendForm below intro blurb with friendCount from useGetFriendsQuery
  </acceptance_criteria>
  <action>
    Mirror AddDeedForm reveal pattern per D-07. Use TextField for tag, PrimaryButton for submit. On catch extract RTK error status and data.message — inline only for 400/404/409 per D-20/D-21; let baseApi banner handle 5xx. Wire on friends page between intro and FriendsList.
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
    <automated>rg -q "normalizeTag" frontend/src/components/friends/AddFriendForm.tsx && rg -q "AddFriendForm" frontend/src/app/\(app\)/friends/page.tsx</automated>
  </verify>
  <done>User can add friend by tag with normalized input and inline validation/conflict errors</done>
</task>

<task type="auto">
  <name>Task 3: Remove friend modal, block section, FriendCard Remove control</name>
  <files>frontend/src/components/friends/FriendCard.tsx, frontend/src/components/friends/RemoveFriendModal.tsx, frontend/src/components/friends/BlockIncomingSection.tsx, frontend/src/components/friends/BlockIncomingModal.tsx, frontend/src/app/(app)/friends/page.tsx</files>
  <read_first>frontend/src/components/deeds/DeleteDeedModal.tsx, .planning/phases/03-friends-shared-deeds/03-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - FriendCard gains destructive TextButton Remove with e.preventDefault and e.stopPropagation so Link navigation does not fire per PATTERNS
    - RemoveFriendModal: title Remove friend?, body You won't see their deeds anymore., buttons Remove friend / Keep friend per D-08 and UI-SPEC; role=dialog, aria-modal, Escape dismiss, focus secondary on mount
    - BlockIncomingSection below list with section-gap: heading Block someone who added you per D-09; tag TextField; Block button opens modal per D-10 — no direct submit
    - BlockIncomingModal: title Block @{tag}?, confirm Block @{tag}, cancel Keep per D-11; calls revokeIncoming with normalizeTag(tag)
    - Block 404 → fixed inline No one with that tag has added you per D-12 — never distinguish unknown tag vs no incoming friendship
    - Friends page renders BlockIncomingSection after FriendsList
    - Modal max-w-[400px]; no OK/Cancel/Revoke labels
  </acceptance_criteria>
  <action>
    Extend FriendCard with onRemove callback opening RemoveFriendModal with friendship _id. Clone DeleteDeedModal shell for both modals. BlockIncomingSection holds tag state and opens BlockIncomingModal on Block click per D-10. On revokeIncoming 404 set D-12 generic inline under block field — ignore API Friendship not found text for user-facing copy (anti-enumeration). Wire remove flow on friends page with modal state for selected FriendItem.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q "Remove friend" frontend/src/components/friends/RemoveFriendModal.tsx && rg -q "Block someone who added you" frontend/src/components/friends/BlockIncomingSection.tsx && rg -q "stopPropagation" frontend/src/components/friends/FriendCard.tsx</automated>
  </verify>
  <done>Remove outgoing and block incoming flows work with confirmation modals and correct inline errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Mutations send normalized tag; server validates @IsTag() |
| UI→user | Destructive actions require explicit modal confirmation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | addFriend tag input | mitigate | normalizeTag before POST; client TAG_PATTERN hint; server validates |
| T-03-05 | Elevation | removeFriend friendshipId | mitigate | Backend scopes DELETE to owner; pass _id from list response only |
| T-03-06 | Information Disclosure | revokeIncoming 404 | mitigate | D-12 generic inline — no tag existence leak |
| T-03-07 | Denial of Service | Accidental remove/block | mitigate | D-08/D-10 confirmation modals before DELETE |
| T-03-SC | Tampering | npm installs | accept | No new packages this plan |
</threat_model>

<verification>
- npm run build and npm run lint pass
- Add duplicate friend → 409 inline under tag (not banner)
- Add unknown tag → 404 inline under tag
- Add self → 400 inline under tag
- Remove friend requires modal; confirm removes row after 204
- Block with no incoming → D-12 generic inline message
</verification>

<success_criteria>
- FRND-02, FRND-03, FRND-04 satisfied on /friends
- List refreshes after add/remove/block via RTK Query invalidation
</success_criteria>

<output>
Create `.planning/phases/03-friends-shared-deeds/03-02-SUMMARY.md` when done
</output>

---
phase: 03-friends-shared-deeds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/types/friend.ts
  - frontend/src/lib/utils/normalize-tag.ts
  - frontend/src/store/baseApi.ts
  - frontend/src/store/friendsApi.ts
  - frontend/src/store/index.ts
  - frontend/src/components/friends/FriendCard.tsx
  - frontend/src/components/friends/FriendsList.tsx
  - frontend/src/app/(app)/friends/page.tsx
autonomous: true
requirements:
  - FRND-01
must_haves:
  truths:
    - "D-01: Brief intro blurb at top of /friends explains directed friendships"
    - "D-02: Plain copy — adding lets you see their deeds; they won't see yours unless they add you"
    - "D-03: Outgoing friend rows show subtle You follow label"
    - "D-05: Friends render as card rows with rounded border matching DeedCard surfaces"
    - "D-06: @tag primary, displayName secondary muted on each card"
    - "D-13: Click entire friend card navigates to /friends/[tag]"
    - "D-17: Initial friends list fetch shows centered Loading…"
    - "D-18: Zero friends shows empty state with prompt toward add form"
    - "User sees friends list with tag and displayName on /friends"
    - "List fetch failure shows explicit error — not empty state (02-REVIEW / D-22 discretion)"
  artifacts:
    - path: frontend/src/lib/types/friend.ts
      provides: UserByTag and FriendItem types mirroring backend DTOs
      contains: FriendItem
    - path: frontend/src/store/friendsApi.ts
      provides: getFriends query
      contains: getFriends
    - path: frontend/src/components/friends/FriendCard.tsx
      provides: Clickable card row with tag, displayName, You follow badge
      contains: You follow
    - path: frontend/src/app/(app)/friends/page.tsx
      provides: Live friends page replacing stub
      contains: Friends
  key_links:
    - from: frontend/src/app/(app)/friends/page.tsx
      to: GET /api/friends
      via: useGetFriendsQuery
      pattern: useGetFriendsQuery
    - from: frontend/src/components/friends/FriendCard.tsx
      to: /friends/[tag]
      via: Link href
      pattern: /friends/
---

## Phase Goal

**As a** signed-in user, **I want to** see who I follow and understand one-way friendships, **so that** I know whose deeds I can view before adding or managing friends.

<objective>
Deliver read path: friend types, normalizeTag utility, RTK Query getFriends, intro blurb, friend cards with navigation link, loading/error/empty states per UI-SPEC.

Purpose: Satisfy FRND-01 and establish list foundation for add/remove/block (Plan 02) and friend deeds (Plan 03).
Output: Live `/friends` list replacing Phase 1 stub; cards link to `/friends/[tag]` (destination implemented in Plan 03).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-friends-shared-deeds/03-CONTEXT.md
@.planning/phases/03-friends-shared-deeds/03-UI-SPEC.md
@.planning/phases/03-friends-shared-deeds/03-PATTERNS.md
@.planning/phases/02-my-deeds/02-REVIEW.md
@backend/src/friends/friends.controller.ts
@backend/src/friends/dto/friend-item.dto.ts
@backend/src/common/dto/user-by-tag.dto.ts
@backend/src/common/utils/normalize-tag.ts
@frontend/src/app/(app)/deeds/page.tsx
@frontend/src/components/deeds/DeedCard.tsx
@frontend/src/store/deedsApi.ts

<interfaces>
UserByTag: { _id: string; displayName: string; tag: string } — no email
FriendItem: { _id: string; friend: UserByTag; createdAt: string }
GET /api/friends → 200 FriendItem[] (outgoing friendships only, newest first)
normalizeTag: trim, lowercase, strip leading @
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Friend types, normalizeTag, friendsApi getFriends, store wiring</name>
  <files>frontend/src/lib/types/friend.ts, frontend/src/lib/utils/normalize-tag.ts, frontend/src/store/baseApi.ts, frontend/src/store/friendsApi.ts, frontend/src/store/index.ts</files>
  <read_first>frontend/src/lib/types/deed.ts, frontend/src/store/deedsApi.ts, backend/src/common/utils/normalize-tag.ts, .planning/phases/03-friends-shared-deeds/03-PATTERNS.md</read_first>
  <acceptance_criteria>
    - UserByTag and FriendItem exported from friend.ts; AddFriendBody stub optional for Plan 02
    - normalizeTag mirrors backend: trim, toLowerCase, replace(/^@/, '')
    - TAG_PATTERN constant exported from normalize-tag.ts or friend.ts for client hint (^[a-z0-9_]{3,32}$)
    - baseApi tagTypes extended: Friend, FriendDeed (FriendDeed used in Plan 03)
    - friendsApi injects getFriends query → GET friends, providesTags [{ type: Friend, id: LIST }]
    - index.ts side-effect imports ./friendsApi
    - export useGetFriendsQuery
  </acceptance_criteria>
  <action>
    Create type file mirroring FriendItemDto and UserByTagDto — never include email on friend surfaces (privacy). Add normalizeTag utility per D-04 foundation. Extend baseApi tagTypes. Inject getFriends only in this plan; mutations added in Plan 02. Register friendsApi via side-effect import matching deedsApi pattern.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q "getFriends" frontend/src/store/friendsApi.ts && rg -q "FriendItem" frontend/src/lib/types/friend.ts && rg -q "normalizeTag" frontend/src/lib/utils/normalize-tag.ts</automated>
  </verify>
  <done>getFriends query registered; types and normalizeTag available to components</done>
</task>

<task type="auto">
  <name>Task 2: FriendCard and FriendsList components</name>
  <files>frontend/src/components/friends/FriendCard.tsx, frontend/src/components/friends/FriendsList.tsx</files>
  <read_first>frontend/src/components/deeds/DeedCard.tsx, frontend/src/components/deeds/DeedList.tsx, .planning/phases/03-friends-shared-deeds/03-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - FriendCard: rounded-lg border card surface per D-05; @tag semibold primary, displayName muted secondary per D-06
    - You follow pill badge per D-03 (text-xs uppercase, bg-zinc-100 dark:bg-zinc-800)
    - Entire card navigates via Link to /friends/{friend.tag} per D-13; min-h-[56px] touch target
    - No Remove button yet — added in Plan 02 with stopPropagation
    - FriendsList: vertical gap-4 stack; empty state heading No friends yet + body Add someone by tag to see their deeds per UI-SPEC when friends.length === 0
    - List component does not handle isLoading/isError — parent page owns those guards
  </acceptance_criteria>
  <action>
    Build FriendCard as clickable card row using Next.js Link wrapping card content. Match DeedCard border/padding tokens from UI-SPEC. FriendsList maps FriendItem[] to FriendCard; renders empty state only when parent passes empty array after successful fetch (not on error).
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
    <automated>rg -q "You follow" frontend/src/components/friends/FriendCard.tsx && rg -q "No friends yet" frontend/src/components/friends/FriendsList.tsx</automated>
  </verify>
  <done>FriendCard and FriendsList render tag, displayName, badge, and empty state copy</done>
</task>

<task type="auto">
  <name>Task 3: Friends page with intro, loading, error, and live list</name>
  <files>frontend/src/app/(app)/friends/page.tsx</files>
  <read_first>frontend/src/app/(app)/deeds/page.tsx, .planning/phases/02-my-deeds/02-REVIEW.md CR-01, .planning/phases/03-friends-shared-deeds/03-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - Page title Friends at text-[28px] font-semibold per UI-SPEC (not deeds text-xl)
    - Intro blurb max-w-xl with D-02 copy per D-01: Adding someone lets you see their deeds. They won't see yours unless they add you.
    - isLoading → centered Loading… under title per D-17
    - isError → explicit Couldn't load friends. Try again. — NOT empty state (02-REVIEW CR-01, D-22 discretion)
    - Successful fetch → intro + FriendsList; placeholder comment or minimal stub for AddFriendForm and BlockIncomingSection (Plan 02 wires these)
    - use client directive; useGetFriendsQuery from friendsApi
  </acceptance_criteria>
  <action>
    Replace friends/page.tsx stub with live page mirroring deeds/page.tsx loading/error trilogy. Add directed-friendship intro blurb per D-01/D-02 above list. Do NOT render empty friends list on isError. Add form and block section come in Plan 02 — leave gap-6 stack space or comment only. FriendCard links to /friends/[tag] which Plan 03 implements.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q "isError" frontend/src/app/\(app\)/friends/page.tsx && rg -q "Couldn't load friends" frontend/src/app/\(app\)/friends/page.tsx</automated>
  </verify>
  <done>Authenticated user sees live friends list, intro blurb, loading and error states; empty state when list is truly empty</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Bearer JWT on GET /api/friends; response must not expose email |
| client→UI | Friend surfaces render UserByTag fields only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | FriendItem display | mitigate | Type UserByTag excludes email; never map or render email from API |
| T-03-02 | Tampering | Tag in URL /friends/[tag] | mitigate | normalizeTag applied before API calls in Plan 03; export utility now |
| T-03-03 | Spoofing | GET /api/friends | transfer | Bearer JWT via baseQueryWithReauth; 401 clears session |
| T-03-SC | Tampering | npm installs | accept | No new packages this plan |
</threat_model>

<verification>
- npm run build and npm run lint pass in frontend
- /friends shows Loading… then list or empty state when API healthy
- /friends shows error message (not empty list) when GET /api/friends fails
- Friend cards show @tag, displayName, You follow badge; link href points to /friends/{tag}
</verification>

<success_criteria>
- FRND-01 satisfied: user sees friends with tag and displayName
- Foundation ready for Plan 02 mutations and Plan 03 friend deeds route
</success_criteria>

<output>
Create `.planning/phases/03-friends-shared-deeds/03-01-SUMMARY.md` when done
</output>

---
phase: 03-friends-shared-deeds
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - frontend/src/store/friendsApi.ts
  - frontend/src/store/usersApi.ts
  - frontend/src/store/index.ts
  - frontend/src/components/deeds/DeedCard.tsx
  - frontend/src/components/friends/FriendDeedList.tsx
  - frontend/src/app/(app)/friends/[tag]/page.tsx
autonomous: true
requirements:
  - FRND-05
  - FRND-06
must_haves:
  truths:
    - "D-14: Friend deeds mirror My Deeds Planned/Done sections with read-only cards"
    - "D-15: 403 shows dedicated error You can't view this user's deeds with Back to friends link — no silent redirect"
    - "D-16: Page header @{tag}'s deeds with displayName subtitle"
    - "D-19: Friend with zero deeds shows No deeds yet — no add form"
    - "D-17: Friend deeds page shows centered Loading… while fetching"
    - "D-22: Non-403 fetch errors show banner or inline page message; 403 uses D-15 only"
    - "User can open /friends/[tag] and see friend's deeds without email or private fields"
    - "Non-friend or unknown tag on deeds fetch shows unified 403 error state — not user not found"
  artifacts:
    - path: frontend/src/app/(app)/friends/[tag]/page.tsx
      provides: Friend deeds page with 403 branch
      contains: You can't view this user's deeds
    - path: frontend/src/components/friends/FriendDeedList.tsx
      provides: Planned/Done read-only sections
      contains: Planned
    - path: frontend/src/store/friendsApi.ts
      provides: getFriendDeeds query
      contains: getFriendDeeds
    - path: frontend/src/components/deeds/DeedCard.tsx
      provides: readOnly prop hiding edit/delete/status actions
      contains: readOnly
  key_links:
    - from: frontend/src/app/(app)/friends/[tag]/page.tsx
      to: GET /api/friends/:tag/deeds
      via: useGetFriendDeedsQuery
      pattern: getFriendDeeds
    - from: frontend/src/app/(app)/friends/[tag]/page.tsx
      to: GET /api/users/by-tag/:tag
      via: useGetUserByTagQuery
      pattern: users/by-tag
    - from: frontend/src/components/friends/FriendDeedList.tsx
      to: DeedCard
      via: readOnly prop
      pattern: readOnly
---

## Phase Goal

**As a** user who follows someone, **I want to** open their deeds page and browse planned and done items read-only, **so that** I can see their good deeds without accessing private information.

<objective>
Add getFriendDeeds query, optional usersApi for displayName on direct URL visits, read-only DeedCard, FriendDeedList sections, and /friends/[tag] page with 403 dedicated state.

Purpose: Satisfy FRND-05 and FRND-06; complete navigation from FriendCard (Plan 01).
Output: Working friend deeds view with privacy-safe error handling per backend anti-enumeration.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-friends-shared-deeds/03-CONTEXT.md
@.planning/phases/03-friends-shared-deeds/03-UI-SPEC.md
@.planning/phases/03-friends-shared-deeds/03-PATTERNS.md
@backend/src/friends/friends.controller.ts
@backend/src/friends/friends.service.ts
@backend/test/friends.e2e-spec.ts
@frontend/src/components/deeds/DeedList.tsx
@frontend/src/components/deeds/DeedCard.tsx
@frontend/src/lib/types/deed.ts
@frontend/src/store/authApi.ts

<interfaces>
GET /api/friends/:tag/deeds → 200 DeedPublic[]; 403 unified for non-friend AND unknown tag
Backend 403 message: You can only view deeds of your friends — UI shows D-15 copy instead
GET /api/users/by-tag/:tag → 200 UserByTag (no email); 404 unknown tag
DeedPublic: same shape as own deeds — reuse from @/lib/types/deed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getFriendDeeds query, usersApi getUserByTag, DeedCard readOnly</name>
  <files>frontend/src/store/friendsApi.ts, frontend/src/store/usersApi.ts, frontend/src/store/index.ts, frontend/src/components/deeds/DeedCard.tsx</files>
  <read_first>frontend/src/store/friendsApi.ts, frontend/src/store/deedsApi.ts, frontend/src/components/deeds/DeedCard.tsx, .planning/phases/03-friends-shared-deeds/03-PATTERNS.md</read_first>
  <acceptance_criteria>
    - getFriendDeeds query GET friends/:tag/deeds with encodeURIComponent(tag); providesTags FriendDeed id tag
    - export useGetFriendDeedsQuery
    - usersApi injects getUserByTag GET users/by-tag/:tag; export useGetUserByTagQuery; index.ts imports ./usersApi
    - DeedCard accepts optional readOnly?: boolean — when true render title, StatusBadge, description only; hide edit/delete/status action row per D-14
    - readOnly mode: no isEditing/onStartEdit/onDelete props required — use discriminated props or make action props optional when readOnly
    - Existing /deeds DeedCard usage unchanged (readOnly false/default)
  </acceptance_criteria>
  <action>
    Add getFriendDeeds to friendsApi. Create usersApi.ts mirroring authApi inject pattern for displayName subtitle on direct /friends/[tag] visits per CONTEXT discretion — prefer fetch over list navigation state. Extend DeedCard with readOnly branch reusing view-mode layout from Plan 2 — single component per PATTERNS discretion, not duplicate FriendDeedCard. Register usersApi side-effect import.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q "getFriendDeeds" frontend/src/store/friendsApi.ts && rg -q "readOnly" frontend/src/components/deeds/DeedCard.tsx && rg -q "getUserByTag" frontend/src/store/usersApi.ts</automated>
  </verify>
  <done>API queries and read-only deed card mode ready for friend deeds page</done>
</task>

<task type="auto">
  <name>Task 2: FriendDeedList with Planned/Done sections</name>
  <files>frontend/src/components/friends/FriendDeedList.tsx</files>
  <read_first>frontend/src/components/deeds/DeedList.tsx, .planning/phases/03-friends-shared-deeds/03-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - Split deeds into Planned and Done sections matching DeedList pattern per D-14
    - gap-6 between sections per UI-SPEC section-gap
    - Map each deed to DeedCard with readOnly prop — no edit/delete modals or status buttons
    - Hide section when zero items in that status (mirror DeedList D-22 analog)
    - Empty array (both sections empty): render nothing — parent page shows D-19 No deeds yet
  </acceptance_criteria>
  <action>
    Clone DeedList section structure without edit/delete modal state. Filter planned/done identically to DeedList. Use readOnly DeedCard for each item. Component receives DeedPublic[] prop only.
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
    <automated>rg -q "readOnly" frontend/src/components/friends/FriendDeedList.tsx && rg -q "Planned" frontend/src/components/friends/FriendDeedList.tsx</automated>
  </verify>
  <done>FriendDeedList renders read-only Planned/Done sections</done>
</task>

<task type="auto">
  <name>Task 3: /friends/[tag] page with header, 403 state, loading, empty</name>
  <files>frontend/src/app/(app)/friends/[tag]/page.tsx</files>
  <read_first>frontend/src/app/(app)/deeds/page.tsx, .planning/phases/03-friends-shared-deeds/03-PATTERNS.md 403 branch, .planning/phases/03-friends-shared-deeds/03-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - use client; useParams from next/navigation; normalizeTag(params.tag) before all API calls
    - useGetFriendDeedsQuery(normalizedTag) and useGetUserByTagQuery(normalizedTag) in parallel
    - isLoading → centered Loading… per D-17
    - error status === 403 → dedicated centered panel: h2 You can't view this user's deeds per D-15; TextButton or Link Back to friends → /friends — do NOT show User not found or raw API 403 message (anti-enumeration FRND-06)
    - Other isError (5xx/network) → inline or rely on ErrorBanner per D-22 — not 403 copy
    - Success header: @{tag}'s deeds (text-xl semibold) + displayName subtitle from getUserByTag when available per D-16
    - deeds.length === 0 → No deeds yet empty state per D-19 — no AddDeedForm
    - Success with deeds → FriendDeedList
    - Never render email anywhere on this page
  </acceptance_criteria>
  <action>
    Create dynamic route page under (app)/friends/[tag]. Normalize route param before queries. Branch on RTK error.status === 403 first for D-15 unified state — backend returns same 403 for non-friend and unknown tag; never map to 404 user-facing copy per FRND-06 and DECISIONS anti-enumeration. Fetch displayName via usersApi for direct URL visits; if getUserByTag 404 while deeds 403, still show 403 deeds state (deeds query is authorization source of truth). Wire FriendDeedList when 200 with data.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <automated>rg -q "403" frontend/src/app/\(app\)/friends/\[tag\]/page.tsx && rg -q "You can't view this user's deeds" frontend/src/app/\(app\)/friends/\[tag\]/page.tsx && rg -q "normalizeTag" frontend/src/app/\(app\)/friends/\[tag\]/page.tsx</automated>
  </verify>
  <done>Clicking friend card opens read-only deeds page; non-friend/unknown tag shows unified 403 error with back link</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | GET friend deeds requires friendship; 403 unified response |
| API→UI | Must not leak tag existence via different error copy for 403 vs 404 on deeds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | getFriendDeeds 403 | mitigate | D-15 unified copy only; never show User not found for deeds 403 (FRND-06) |
| T-03-09 | Information Disclosure | UserByTag on page | mitigate | Render displayName and tag only; never email |
| T-03-10 | Tampering | [tag] route param | mitigate | normalizeTag before API; encodeURIComponent in query URLs |
| T-03-11 | Elevation | Read-only deeds | mitigate | DeedCard readOnly hides all mutation controls; no PATCH/DELETE from UI |
| T-03-SC | Tampering | npm installs | accept | No new packages this plan |
</threat_model>

<verification>
- npm run build and npm run lint pass
- Friend with deeds: /friends/{tag} shows Planned/Done read-only sections
- Non-friend or unknown tag: 403 panel with Back to friends — same copy for both cases
- Friend with zero deeds: No deeds yet empty state
- Direct URL /friends/{tag} loads displayName subtitle via getUserByTag when authorized
</verification>

<success_criteria>
- FRND-05 and FRND-06 satisfied
- Phase 3 complete: manage friends + view friend deeds end-to-end
</success_criteria>

<output>
Create `.planning/phases/03-friends-shared-deeds/03-03-SUMMARY.md` when done
</output>
